# Science/PNAS requests are disabled until you set all three values explicitly.
# These are conservative starting points, not publisher-approved quotas.

FLARESOLVERR_MIN_INTERVAL_SECONDS=20
FLARESOLVERR_MAX_REQUESTS_PER_HOUR=30
FLARESOLVERR_MAX_REQUESTS_PER_DAY=200
