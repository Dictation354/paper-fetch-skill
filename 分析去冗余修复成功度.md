> From: https://chatgpt.com/c/6a9a56bc-a288-83ec-b7ef-030bd9bfcfe2

# chatgpt response

我会按“**还能删掉而不损失行为/可维护性**”来找，而不是把所有重复代码都算冗余。重点看当前 `main` 的运行时 wrapper、provider boilerplate、重复 normalization/cache/asset 逻辑，以及仓库里是否还残留一次性诊断产物。

我已经找到两类“还会继续制造 bug 的冗余”：一类是 **同一输入规范化逻辑仍有多份实现**，另一类是 **provider 已有 catalog 事实源，但部分 provider 仍自己重新判断 host/source**。这两类比单纯重复几行代码更值得处理。

有。**但现在剩下的已经不是上一轮那种“大量架构性冗余”，而主要是兼容层、局部重复 contract、旧命名和仓库维护面。**我看完当前 `main` 后，认为还有 6 类值得处理，其中前 3 类是真正建议继续清掉的。

| 优先级 | 剩余冗余 | 判断 |
|---|---|---|
| **P1** | `_ams_html` / `_mdpi_html` / `_springer_html` compatibility facade | **明确冗余，建议删** |
| **P1** | MCP optional-string normalization 多份实现 | **明确冗余，而且已经导致过 bug** |
| **P1/P2** | provider `article_source_for_payload()` 重复 | **适合下沉到 BrowserWorkflowClient/profile** |
| **P2** | provider 自己重复判断 domain/landing URL | **可继续向 catalog 收敛，但不能粗暴统一** |
| **P2** | Browser runtime 旧 Playwright 命名和多层转发 | **有冗余，但保留 facade 本身是正确的** |
| **P2/P3** | 当前 contract 文档、live artifact 重复 | **维护冗余，值得清理** |

### 1. 最明显的是三个“明牌 compatibility facade”

仓库自己已经把它们写成：

- `_ams_html.py`：`Compatibility facade for AMS provider HTML helpers`
- `_mdpi_html.py`：`Compatibility facade for MDPI provider HTML helpers`
- `_springer_html.py`：`Compatibility facade for SPRINGER provider HTML helpers`。  

其中 `_mdpi_html.py` 尤其典型：自己几乎不拥有逻辑，只是把 `_mdpi_dom`、`_mdpi_assets`、`_mdpi_markdown`、`_mdpi_authors`、`_mdpi_references` 再 re-export 一次，并提供一个 `__getattr__` 转发。

更关键的是，文档已经明确承认 `_mdpi_html` 只是 compatibility facade，真正 canonical owner 是拆分后的模块；但生产 `mdpi.py`、provider tests 和 golden corpus 仍然通过 facade 调用。 

所以这一块基本没有设计争议：

> **把内部生产代码和测试迁到 canonical module → 删除三个 facade。**

这应该是下一轮最安全、最纯粹的去冗余。

---

### 2. `mcp/schemas.py` 还有一次非常值得做的小收敛

现在同一个“optional string”的语义仍写了多遍：

```text
None → None
str(value).strip()
空字符串 → None
```

分别出现在：

- `_FetchOptionsRequest.normalize_optional_string`
- `FetchPaperToolRequest.normalize_download_dir`
- `BatchFetchRequest.normalize_batch_optional_string`
- `ListCachedRequest.normalize_download_dir`
- `GetCachedRequest.normalize_download_dir`
- `BrowserPreflightRequest.normalize_storage_state_path`。 

这个我会列为 **P1**，原因不是它占多少行，而是 **上一轮刚刚出现过 single fetch 与 batch fetch normalization 漂移的 bug**。

现在虽然已经修成一致，但实现仍然是多份，所以未来仍可能再次漂移。

建议只增加一个非常小的函数：

```python
def _normalize_optional_stripped_string(value: Any) -> str | None:
    if value is None:
        return None
    return str(value).strip() or None
```

然后所有这些 validator 都调用它。

**不要为此建新 abstraction class。一个函数就够。**

---

### 3. `article_source_for_payload()` 是现在最明显的一批 provider boilerplate

当前 AIP、AMS、MDPI、T&F、IOP、Annual Reviews、Royal Society Publishing 等都存在自己的：

```python
def article_source_for_payload(...):
    route = ...
    if route == PDF_FALLBACK:
        return "<provider>_pdf"
    return "<provider>_html"
```

搜索可以直接看到这些几乎同构的 override。    

例如 Annual Reviews 就是标准的这一模式。

而 `BrowserWorkflowClient` 基类已经有 `article_source_for_payload()`，只是目前完全忽略 payload，直接返回普通 article source。

所以这里很适合改成：

- profile 声明 `html_source_name`
- profile 声明 `pdf_source_name`
- base client 根据 `route_kind` 选择

甚至如果 `<provider>_html` / `<provider>_pdf` 已经是强约定，可以从 profile/catalog 直接派生。

这样可以一次删掉一批重复 override，而且以后新增 browser provider 不需要再复制这段。

我会把它列 **P1/P2**。

---

### 4. provider 仍然存在“catalog 已经知道，但 provider 又判断一遍”的情况

比如 AIP 的 `ProviderSpec` 已经声明：

```python
domains=("pubs.aip.org",)
```

但 `AipClient.html_candidates()` 仍通过 `_is_aip_url()` 自己解析 URL，然后再次：

```python
host == "pubs.aip.org" or host.endswith(".pubs.aip.org")
```



T&F 也存在相同模式。

与此同时 catalog 已经提供：

- `host_matches_domain()`
- `matching_provider_domain()`
- `provider_domain_matches()`

作为共享 owner。

所以确实还有进一步收敛空间。

不过这里**不建议直接把所有 `_is_xxx_url()` 全部替换成 `provider_domain_matches()`**。原因是 catalog 的 `domains` 有时是身份识别域集合，而 acquisition landing URL 可能需要更严格的 host 集合。

比如 ACS：

```text
domains = www.acs.org / pubs.acs.org / acs.org
base_domains = pubs.acs.org
```

显然“这是 ACS 域”与“这是允许作为论文正文 landing 的 ACS 域”不是完全同一个概念。

更好的做法是：

> **复用统一 `host_matches_domain()`，但 host 集合仍由具体 route/base_domains 决定。**

这样去掉重复 URL/domain 算法，而不是抹掉 provider 语义。

---

### 5. Browser 层还有一些 v3/v4 时代留下的旧命名和双层 facade

现在文档明确说 **唯一 browser backend 是 Camoufox**，provider 必须通过 `browser_runtime` facade。

这个 facade 我认为**应该保留**。

但内部目前是：

```text
browser_runtime/__init__.py
        ↓ lazy __getattr__
browser_runtime/api.py
        ↓ 所有函数直接转发
DEFAULT_CAMOUFOX_BACKEND
        ↓
backends/camoufox.py
```

`api.py` 的 `load_runtime_config`、`ensure_runtime_ready`、`probe_runtime_status`、`fetch_html_with_browser`、`warm_browser_context` 等实际上全部直接 delegate 到唯一的 `DEFAULT_CAMOUFOX_BACKEND`。

同时 `__init__.py` 又动态 lazy-export `api.py`。

所以：

> **“provider → browser facade” 是必要边界；  
> “facade → generic API → 单一 backend” 是否还需要两层，是可以重新考虑的。**

另外还有更明确的遗留：

```python
fetch_pdf_with_playwright = fetch_pdf_with_browser
```

而 BrowserWorkflow 又把 `fetch_pdf_with_browser` 映射回这个旧名字；IEEE 和测试里还专门 patch `fetch_pdf_with_playwright`。  

以及：

```python
PlaywrightRuntimeConfig = BrowserRuntimeConfig
PlaywrightBrowserFailure = BrowserRuntimeFailure
```



这些旧别名可以逐步删掉。

但是这一块我建议**最后做**，因为 browser 层最近刚修过 ACS/AIP，没必要为了几十行再扰动一次生命周期代码。

---

### 6. `browser_workflow/__init__.py` 的 re-export 面还是偏大

现在这个文件用一个很大的 `_EXPORTS` 表，把：

- profile
- client
- assets
- fetchers
- bootstrap
- html extraction
- PDF fallback
- browser runtime

甚至大量 `_private_name` 再统一 re-export 一遍。

这不是 bug，但它会产生一个问题：

```text
canonical owner
     ↓
browser_workflow facade
     ↓
provider / tests
```

久而久之就又容易分不清函数真正属于哪里。

我不会删整个 facade，建议只是**缩小 public surface**：

保留类似：

```text
BrowserWorkflowClient
BrowserWorkflowPolicy
ProviderBrowserProfile
make_atypon_browser_profile
```

真正属于 `assets` / `fetchers` / `html_extraction` 的内部函数直接从 canonical submodule 引入。

属于 **P2/P3**。

---

### 7. 仓库里的 `artifacts/` 基本已经成为一次性证据冗余

当前还提交着：

- `aip-crossmark-20260902/`
- `live-provider-analysis-20260902/`
- `live-provider-analysis-20260902-fixed/`
- 两份对应 XML。

而搜索这些名字，主要命中的都是 artifact 自己内部对自己的引用，没有看到正常 runtime/test/docs 把它们当稳定 fixture 使用。 

这些在修 ACS/AIP 时作为证据非常有价值，但现在相关行为已经有正式 regression tests。

所以我建议：

```text
需要长期 replay 的最小样本
        ↓
tests/fixtures

一次 live investigation 的完整输出
        ↓
不要 commit
```

而且 `.gitignore` 现在已经明确把 `failures/` 当成 ad-hoc debugging output，却没有忽略 `artifacts/`。

这一块可以直接清。

---

### 8. 文档里仍然复制了太多“当前数字 contract”

例如当前 asset budget 的：

```text
32 MiB / file
256 MiB total
64 MP
4 workers
arXiv 128 members
```

同时出现在：

- `docs/cli.md`
- `docs/architecture/overview.md`
- `docs/providers.md`
- skill `references/presets.md`
- skill `references/tool-contract.md`。   

这就是为什么上次 `128 → None` 时要同时修改一堆文档。

Changelog 里的历史数字当然不能删；但**current contract** 最好只有一个详细 owner，其余地方只描述语义并链接过去。

---

## 所以我现在会怎么清

我不建议再来一次 `be4c489` 那种全仓大爆破。

下一轮最合理的是三个很小的 PR：

1. **compatibility cleanup**：迁移并删除 `_ams_html` / `_mdpi_html` / `_springer_html`，顺带删除明显的 Playwright compatibility aliases。
2. **contract dedup**：统一 MCP optional-string normalization；把 browser provider 的 `article_source_for_payload()` 下沉到 base/profile。
3. **repository hygiene**：删除一次性 `artifacts/`，减少 current-contract 文档中的数字复制。

做完这三轮后，我认为 **paper-fetch-skill 的“真实冗余”就已经很少了**。剩下类似 provider-specific extraction、asset hooks、waterfall 差异，即使看起来重复，也多数属于出版社真实差异，不应该继续为了 DRY 强行抽象。

其中我最推荐你**马上处理前两项**：`compatibility facade` 和 `schemas.py normalization`。它们现在已经是“收益明确、风险很低、不会重新引入过度设计”的清理点。